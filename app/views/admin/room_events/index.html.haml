.row
  .col-md-12
    .page-header
    %h1
      Conference Room List
    %p.text-muted
      Display Event Activity in a Room
- if @rooms.any?
  .row
    .col-md-12
      %table.table.table-hover#rooms
        %thead
          %th Name
        %tbody
          - @rooms.each do |room|
            %tr
              %td
                = link_to room.name, admin_conference_room_event_path(room.venue.conference.short_title, room)
